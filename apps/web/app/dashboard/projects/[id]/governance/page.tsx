import { ShieldHalf } from "lucide-react";
import { SectionStub } from "@/components/layout/SectionStub";

export default function ProjectGovernancePage() {
  return <SectionStub title="Governance" description="Model rules, routing, and guardrails — project overrides on top of org defaults." icon={ShieldHalf} phase="Phase 5" />;
}
