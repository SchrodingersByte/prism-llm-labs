import { KeyRound } from "lucide-react";
import { SectionStub } from "@/components/layout/SectionStub";

export default function ProjectKeysPage() {
  return <SectionStub title="API Keys" description="Prism keys scoped to this project." icon={KeyRound} phase="Phase 4" />;
}
