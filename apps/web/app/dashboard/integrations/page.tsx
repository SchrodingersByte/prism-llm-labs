import { Plug } from "lucide-react";
import { SectionStub } from "@/components/layout/SectionStub";

export default function IntegrationsPage() {
  return (
    <SectionStub
      title="Integrations"
      description="Provider keys, billing APIs, GitHub, Slack, and export destinations."
      icon={Plug}
      phase="Phase 6"
    />
  );
}
